import OpsFunnelClient from "@/components/ops/OpsFunnelClient";

export const metadata = {
  title: "Funnel ops | Qtangl",
};

export default function OpsFunnelPage() {
  return <OpsFunnelClient />;
}
