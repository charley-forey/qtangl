# Supply-chain provenance (E10)

- Cosign image signing for backend/worker containers in CI
- SLSA provenance attestation on release builds
- Sign `qtangl-verify` pip artifact
- Self-SBOM/CBOM published on trust page

Configure in `.github/workflows/` — enable when registry supports cosign.
