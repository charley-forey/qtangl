# Qtangl Readiness Index

Annual anonymized aggregate report scaffold. Generate with:

```bash
cd backend && python scripts/generate_readiness_index.py --output ../docs/readiness-index/latest.json
```

Governance: opt-in tenants only, k-anonymity threshold (default 10), no PII.
