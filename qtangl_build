🧠 CURSOR AGENT PROMPT — QTANGL BUILD

You are a senior full-stack engineer and startup product builder.

Your job is to design and implement the first production-ready MVP website for a startup called Qtangl.

You are building this as if it will be launched publicly within days.

🏢 COMPANY CONTEXT

Company Name: Qtangl
Domain: qtangl.com
Category: Quantum Optimization API Platform

Qtangl helps companies solve:

scheduling optimization
routing optimization
resource allocation problems

It uses hybrid classical + quantum computing (IBM Quantum + Qiskit in backend conceptually, but backend can be mocked for MVP).

🎯 OBJECTIVE

Build a clean, modern, high-conversion landing page website for Qtangl that:

clearly explains the product in under 10 seconds
feels like a real venture-backed deep-tech startup
is simple, minimal, and highly professional
is ready to deploy immediately (Vercel-ready)
⚙️ TECH STACK REQUIREMENTS

Use:

Next.js (App Router)
TypeScript
TailwindCSS
Clean component structure
Responsive design (mobile-first)
No unnecessary dependencies
No backend required (frontend only MVP)

Optional:

Framer Motion for subtle animations (only if lightweight)
🧱 REQUIRED WEBSITE STRUCTURE

Create a single-page landing site with the following sections:

1. HERO SECTION (MOST IMPORTANT)

Must include:

Company name: Qtangl
Tagline (quantum + optimization + clarity)
One sentence explanation
Primary CTA button
Secondary CTA (optional: “View API”)

Example tone:

simple
confident
enterprise-grade
no hype language
2. PROBLEM SECTION

Explain clearly:

Companies struggle to coordinate scheduling + routing across systems
Optimization problems are too complex for manual + basic software tools

Keep it grounded and real.

3. SOLUTION SECTION

Explain:

Qtangl is a quantum optimization API platform
It solves scheduling and routing problems
It converts real-world constraints into optimized outputs

Avoid jargon overload.

4. HOW IT WORKS (3 STEPS)
Input scheduling or routing problem
Qtangl optimizes using hybrid classical + quantum methods (conceptual)
Return optimized plan via API

Keep it visual and simple.

5. USE CASES

Include 3 cards:

Construction scheduling optimization
Logistics routing optimization
Workforce/resource allocation

Each must include:

problem
outcome
value
6. API SECTION (IMPORTANT)

Design a simple developer-style mock API example:

Include:

Example request (JSON)
Example response (JSON)
Simple explanation of flow

Keep it realistic but lightweight (MVP-level mock is fine).

7. CTA SECTION

Include:

“Request Early Access”
email capture UI (simple form UI only, no backend needed)
🎨 DESIGN DIRECTION
dark + minimal + futuristic
high contrast
subtle gradients
clean typography
“deep tech startup” feel (not crypto, not flashy AI)

Inspiration:

Stripe
Vercel
Palantir
OpenAI early landing pages

Avoid:

sci-fi overload
quantum visuals cliché (no glowing atoms everywhere)
🧠 CONTENT RULES
Must be simple enough for a non-technical executive to understand in 10 seconds
No academic quantum language
No unnecessary complexity
Focus on clarity and trust
Position as enterprise optimization infrastructure
🚀 OUTPUT REQUIREMENTS

Generate:

Full Next.js project structure
All page code (single landing page is fine)
Reusable components (Hero, Section, Cards, CTA)
Tailwind styling included
Clean file organization
Production-ready code quality
🧭 SUCCESS CRITERIA

The output is successful if:

The site looks like a real funded startup
The value proposition is instantly clear
A user understands what Qtangl does in <10 seconds
The page is deployable immediately to Vercel
🔥 IMPORTANT THINKING MODE

Before writing code:

prioritize clarity over cleverness
remove anything unnecessary
design like a founder building to launch fast
assume first users are enterprise buyers, not consumers